<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="sync_frequency"
        android:title="@string/Games_time_interval_Array"
        android:entries="@array/Games_time_interval_Array_titles"
        android:entryValues="@array/Games_time_interval_Array_values"
        android:defaultValue="180"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <ListPreference
        android:key="Game_selection"
        android:defaultValue="אנא בחר משחק"
        android:title="בחירת משחק"
        android:entries="@array/Games_Selection_Array_titles"
        android:entryValues="@array/Games_Selection_Array_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
